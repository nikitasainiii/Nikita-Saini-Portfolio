# Web3Forms — https://web3forms.com (free tier). Required for the contact form.
# Copy to .env locally and add the same key in Vercel → Project → Settings → Environment Variables.
VITE_WEB3FORMS_ACCESS_KEY=
